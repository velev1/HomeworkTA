using System;
using System.Collections.Generic;

class PrimeNumbers
{
    static void Main()
    {
        int n = int.Parse(Console.ReadLine());

        bool isPrime = true;
        uint divider = 2;
        uint maxDivider = (uint)Math.Sqrt(n);


        for (int i = n - 1; i >= 0; i--)
        {

            while (true)
            {

            }

        }


        
      
    }
}

